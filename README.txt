Hello World
Today is a sad
sunday

Git command lines

mkdir harika #creates a directory file , shift+zz, type intial like R+tab =readme.txt file
cd harika #get into that directory
git init # initialize it
git config --global user.name "Harika Prasanna"
git config --global user.email "harikaprasanna2024@gmail.com"
git remote add origin https://github.com/Harikaprasanna19/postgres
rm -rf postgres #removes the file which has error
git clone https://github.com/Harikaprasanna19/postgres.git
cd .. #come out of the directory

git status #gives the status of git
git add . #saves the changed file
git commit -m "add msg" #adds to git
git push #pushes the updated file
docker img: blueprint of a server/ docker container , create server
container : actual server /stores data